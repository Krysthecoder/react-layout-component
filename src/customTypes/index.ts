export type productProps = {
  productType: string;
  productTitle: string;
  productDetails: string;
  currentPrice: string;
  oldPrice: string;
  buttonContent: string;
};

export type ProductDataProps = {
  productType: string;
  productTitle: string;
  productDetails: string;
  currentPrice: number;
  oldPrice: number;
  buttonContent: string;
};